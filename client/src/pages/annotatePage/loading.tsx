import { VscLoading } from 'react-icons/vsc';
import { PanelContainer } from './style';

export const Loading = () => {
  return (
    <PanelContainer>
      <VscLoading />
    </PanelContainer>
  )
}